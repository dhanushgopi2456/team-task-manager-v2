# TEAM TASK MANAGER — Server environment
NODE_ENV=development
PORT=5000

# MongoDB — leave MONGO_URI empty to auto-start an in-memory demo database (dev only)
MONGO_URI=
MONGO_DB_NAME=team_task_manager

JWT_SECRET=change-me-to-a-long-random-string-in-production
CLIENT_URL=http://localhost:5173
